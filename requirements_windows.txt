# Windows 平台依赖包（仅支持 Android 监控）
# iOS 监控功能在 Windows 上不可用

# Web 框架 (Web界面必需)
flask==3.1.0
flask-socketio==5.4.1
python-socketio==5.12.0

# 核心依赖
psutil==7.0.0

# Flask-SocketIO 的底层依赖
python-engineio==4.10.1
simple-websocket==1.1.0
wsproto==1.2.0

# 说明：
# 1. Windows 平台只能使用 Android 监控功能
# 2. 需要安装 Android SDK Platform Tools (ADB)
# 3. 启动命令: python start_android_monitor.py

